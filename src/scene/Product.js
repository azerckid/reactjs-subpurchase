import React from "react";
import styled from "styled-components";
const ProductContainer = styled.div``;

const Product = () => {
  return <ProductContainer>Product</ProductContainer>;
};

export default Product;
